#include "Primitives.imp"

void foo(int[int] a) {
  int i := 0;
  int j := 1;
  a[j] := 0;
  while (i<1000) do {
    a[j] := a[j]+1;
    i := i+1
  };
  ifnd (a[0]<=0) then { verror() } else { void }
}
