#include "Primitives.imp"

int foo(int x, int y) {
 if (x<=y) then {
  if (x>10) then {
   ierror()
  } else { 1 }
 } else {
  if (randBool()) then { ierror() }
  else { ierror() }
 }
} 

{-
void foo (int x, int y, ref int r) {
 y:=y-1; r:=1;
 if (x<=y) then {
  if (x>10) then { ferror() } else { Void }
 } else {
  if (randBool()) then { r:=r+1 } else { ferror() }
 }
}
-}
