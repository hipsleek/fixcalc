#include "Primitives.imp"

void goo(int[int] a,ref int i){
  if (i<=0) then { Void }
  else {
    assign(a,i,0);
    i:=i-1;
    goo(a,i)
  }
}

{-
# NEVER_BUG;

{[a,i]: 1 <= i < a} union
 {[a,i]: i <= 0 && 1 <= a}

# MUST_BUG;

{[a,i]: 1 <= a <= i}
-}